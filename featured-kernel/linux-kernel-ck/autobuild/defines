PKGNAME=linux-kernel-ck5-4.7.5
PKGSEC=kernel
PKGDEP=""
BUILDDEP="bc dracut git"
if [[ "${CROSS:-$ARCH}" = "amd64" ]]; then
    BUILDDEP+=" grub"
fi
PKGDES="Linux Kernel version 4.7.5 with ck5 patchset (including BFS CPU scheduler) and BFQ I/O scheduler, optimized for desktop usage"

ABSTRIP=no
ABELFDEP=no
ABRPMAUTOPROVONLY=yes
