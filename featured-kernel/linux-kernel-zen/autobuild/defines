PKGNAME=linux-kernel-zen-4.8.4
PKGSEC=kernel
PKGDEP=""
BUILDDEP="bc dracut git"
if [[ "${CROSS:-$ARCH}" = "amd64" ]]; then
    BUILDDEP+=" grub"
fi
PKGDES="Linux Kernel version 4.8.4 with ZEN patchset"

ABSTRIP=no
ABELFDEP=no
ABRPMAUTOPROVONLY=yes
