PKGNAME=linux-kernel-broadwell-4.7
PKGSEC=kernel
PKGDEP=""
BUILDDEP="bc dracut git"
if [[ "${CROSS:-$ARCH}" = "amd64" ]]; then
    BUILDDEP+=" grub"
fi
PKGDES="Linux Kernel version 4.7 for desktop usage (for Intel Broadwell)"

ABSTRIP=no
ABELFDEP=no
ABRPMAUTOPROVONLY=yes

FAIL_ARCH=!(amd64)
