PKGNAME=linux-kernel-staging-4.10
PKGSEC=kernel
PKGDEP=""
BUILDDEP="bc dracut git"
if [[ "${CROSS:-$ARCH}" = "amd64" ]]; then
    BUILDDEP+=" grub"
fi
PKGDES="Linux Kernel version 4.10.0 for test use"

ABSTRIP=no
ABELFDEP=no
ABRPMAUTOPROVONLY=yes
